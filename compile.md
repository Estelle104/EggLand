**Si on a modifie pom.xml**
mvn clean install

**Si on veut lancer l'applocation**
mvn spring-boot:run

**url dans la navigateur**
localhost:1404

**CHangement version java**
sudo update-alternatives --config java